    // API Console es una INTERFAZ del BROWSER
    // Sirve para gestionar la consola
    /*
     * F (funciones)
     * E (eventos)
     * O (objetos): console
     * 
     * (al ámbito global)
     */
    console.log(console);

    // API Document es una INTERFAZ del BROWSER -DOM (Document Object Model)-
    // Sirve para gestionar el documento HTML en donde está insertado este script
    /*
     * F (funciones)
     * E (eventos)
     * O (objetos): docuemnt
     * 
     * (al ámbito global)
     */
    console.log(document);

    // API Window es una INTERFAZ del BROWSER -BOM (Browser Object Model)-
    // Sirve para gestionar el ámbito global y propiedades del navegador
    /*
     * F (funciones)
     * E (eventos)
     * O (objetos): window
     * 
     * (al ámbito global)
     */
     console.log(window);
